[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --cov=services
    --cov=config
    --cov=agent
    --cov=api
    --cov-report=term-missing
    --cov-fail-under=90
    -v
asyncio_mode = auto
